# Domain Docs

This is a single-context repository.

## Before exploring

- Read `CONTEXT.md` at the repository root.
- Read ADRs under `docs/adr/` that touch the area being changed.

## Vocabulary

Use the glossary terms defined in `CONTEXT.md` for domain concepts. If output contradicts an ADR, surface the conflict explicitly rather than silently overriding it.
